query Meet($id: ID!) {
    meet(id: $id) {
        title
        description
        dates
        hours
        scheduledTime
        availabilities {
            timeslots
            name
        }
    }
}